import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    menuItems: {
        marginHorizontal: 10,
    },
    menuRow: {
        flexDirection: 'row',
        alignItems: 'center',
    },

    menuTextBox: {
        flex: 1,
    },
    menuText: {
        color: 'white',
    }

});

export default styles;
