import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    menuItems: {
        marginHorizontal: 20,
        marginVertical: 20,
    },
    menuRow: {
        flexDirection: 'row',
        alignItems: 'center',
    },

    menuTextBox: {
        flex: 1,
    },
    menuText: {
        color: 'white',
    }

});

export default styles;
