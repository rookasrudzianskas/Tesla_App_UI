import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    menuItems: {
        marginHorizontal: 20,
        marginVertical: 20,
    },
    menuRow: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 15,
    },

    menuTextBox: {
        flex: 1,
    },
    menuText: {
        color: 'white',
        fontSize: 16,
        fontWeight: 'bold',
        marginLeft: 15,
    }

});

export default styles;
